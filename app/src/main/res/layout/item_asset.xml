<?xml version="1.0" encoding="utf-8"?>

<!--
Card for a single asset item in the list.
- Rounded corners and outline
- Clickable & focusable so it can respond to taps
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="12dp"
    android:layout_marginVertical="0dp"
    app:cardUseCompatPadding="true"
    app:cardPreventCornerOverlap="true"
    app:cardBackgroundColor="?attr/colorSurfaceVariant"
    app:strokeColor="@color/md_sys_color_outline"
    app:strokeWidth="1dp"
    app:shapeAppearanceOverlay="@style/Shape.Rounded.L"
    android:clickable="true"
    android:focusable="true">

    <!--
    Vertical LinearLayout inside the card.
    Holds all asset info with padding around content.
    -->
    <LinearLayout
        android:orientation="vertical"
        android:padding="14dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!--
        Row: ticker (left, bold) and type (right, smaller, lighter).
        -->
        <LinearLayout
            android:orientation="horizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical">

            <!-- Asset ticker symbol -->
            <TextView
                android:id="@+id/tvTicker"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textStyle="bold"
                android:textSize="16sp"
                android:textColor="?attr/colorOnSurface"/>

            <!-- Asset type (e.g., stock, bond) -->
            <TextView
                android:id="@+id/tvType"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="14sp"
                android:textColor="?attr/colorOnSurface"
                android:alpha="0.8"/>
        </LinearLayout>

        <!--
        Row: price (left) and yield (right).
        Spread evenly using weights.
        -->
        <LinearLayout
            android:orientation="horizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:weightSum="2">

            <!-- Asset price -->
            <TextView
                android:id="@+id/tvPrice"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textColor="?attr/colorOnSurface"
                android:text="Price: —"/>

            <!-- Asset yield (aligned to end) -->
            <TextView
                android:id="@+id/tvYield"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="end"
                android:textColor="?attr/colorOnSurface"
                android:text="Yield: —"/>
        </LinearLayout>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
